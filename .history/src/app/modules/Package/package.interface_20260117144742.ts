export type TPackage = {
  name: string;      // jemon: 'Basic Pack', 'Pro Pack'
  credits: number;   // koto credit pabe (e.g., 10, 20)
  price: number;     // koto taka (e.g., 200, 500)
  isActive: boolean;
};